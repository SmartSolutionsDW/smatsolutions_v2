= form_for @tipo_producto do |f|
  - if @tipo_producto.errors.any?
    #error_explanation
      %h2= "#{pluralize(@tipo_producto.errors.count, "error")} prohibited this tipo_producto from being saved:"
      %ul
        - @tipo_producto.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :co_tipoproducto
    = f.number_field :co_tipoproducto
  .field
    = f.label :no_cotipoproducto
    = f.text_field :no_cotipoproducto
  .actions
    = f.submit 'Save'
